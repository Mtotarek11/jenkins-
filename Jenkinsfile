pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                echo 'Hello! Jenkins successfully read the Jenkinsfile from GitHub.'
            }
        }
    }
}
